/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PagesPipe } from './pages.pipe';

describe('Pipe: Pages', () => {
  it('create an instance', () => {
    let pipe = new PagesPipe();
    expect(pipe).toBeTruthy();
  });
});
