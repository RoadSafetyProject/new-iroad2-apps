/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RelationLabelConverterPipe } from './relation-label-converter.pipe';

describe('Pipe: RelationLabelConverter', () => {
  it('create an instance', () => {
    let pipe = new RelationLabelConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
